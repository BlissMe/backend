const express = require('express');
const router = express.Router();

// const { getPunterList } = require('../controllers/mainframe.controller');

// router.get('/api/getPunterList/:PhoneNo', getPunterList);

module.exports = router;